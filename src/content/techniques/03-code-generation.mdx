---
title: "Code Generation"
order: 3
lesson: "lektion-4"
estimatedTime: "4 min"
breadcrumb: "Code Generation"
description: "Få stabil kode med klare krav"
---

## KRAV

Kode‑prompts skal have constraints: sprog, format, filnavn, og edge cases.

### Typiske krav

- Sprog + version
- Input/output format
- Fejlhåndtering


## SAMMENLIGNING

<PromptComparison
  badPrompt={"Skriv en funktion til at validere email"}
  goodPrompt={"Skriv en TypeScript‑funktion \\`isValidEmail(email: string): boolean\\`. Brug regex, ingen eksterne libs. Giv 5 testcases."}
  badExplanation={"Uklarhed giver uens kode."}
  goodExplanation={"Krav giver konsistent output."}
/>

<CopyablePrompt title={"AI‑respons"} prompt={"Skriv en TypeScript‑funktion \\`isValidEmail(email: string): boolean\\`. Brug regex, ingen eksterne libs. Giv 5 testcases."} />

<AISimulation prompt={"Skriv en TypeScript‑funktion \\`isValidEmail(email: string): boolean\\`. Brug regex, ingen eksterne libs. Giv 5 testcases."} response={"export function isValidEmail(email: string): boolean {\n  return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email);\n}\n\n// Tests\nisValidEmail(\"a@b.com\") // true\nisValidEmail(\"test.user@domain.co\") // true\nisValidEmail(\"bad@\") // false\nisValidEmail(\"no-at.com\") // false\nisValidEmail(\"a@b\") // false"} />

<ExerciseBox
  title={"Øvelse (4 minutter)"}
  description={"Vælg en lille kodeopgave og tilføj klare constraints."}
  steps={[
  "Angiv sprog og version",
  "Angiv input/output",
  "Bed om tests"
]}
/>
